# NOTE: In production, manage secrets with Sealed Secrets, Vault, or AWS Secrets Manager.
# Values below are base64-encoded.  Encode with:  echo -n "value" | base64
#   postgres  →  cG9zdGdyZXM=
apiVersion: v1
kind: Secret
metadata:
  name: lojaveloz-secret
  namespace: lojaveloz
type: Opaque
data:
  # echo -n "postgres" | base64
  DB_PASSWORD: cG9zdGdyZXM=
